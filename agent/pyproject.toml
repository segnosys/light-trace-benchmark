[project]
name = "agent-throughput"
version = "0.1.0"
description = "Agent-workload throughput benchmark for LLM inference servers"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.8.0",
    "dash>=2.0.0",
    "plotly>=5.0.0",
    "numpy>=1.20.0",
    "pyyaml>=6.0",
    "transformers>=4.20.0",
]
